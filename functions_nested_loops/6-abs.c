#include "main.h"

/**
 * _abs - compute the absoulte value of integer
 *
 * @n: The number to check
 * Description: compute the absolute value of intger
 *
 * Return: Return positive of an integer
 *
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
